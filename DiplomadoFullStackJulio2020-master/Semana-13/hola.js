const readline = require('readline-sync');



function pedir() {
    let funcion = readline.question('¿Que operacion quieres hacer?');
    var
    if (funcion === a) {
        console.log('es una suma');
    } else if (funcion == b) {
        console.log('es una resta');
    }

}
exports.pedir = pedir;